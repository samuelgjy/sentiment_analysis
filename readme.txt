<!-- in order to run project, ensure a venv is setup,
first create virtual environment using py -3 -m venv .venv using windows cmd in the desired folder, afterwards .venv\scripts\activate to activate the venv

ctrl + shift + p and type 'python intepreter'
select the one that has .venv inside the path

open the terminal using ctrl + shift + `
followed by:
python -m pip install --upgrade pip
python -m pip install flask

when done import the remaining modules:
pip install;
pandas
nltk
spacy
matplotlib
nrclex
plotly
numpy
google
selenium
response
gensim
pyLDAvis
skLearn
bs4
orca
wordcloud
dash
fake_useragent
finally python -m spacy download en_core_web_sm
 -->
